# mynews — 부동산 인사이트 (데일리 키워드 리포트)

GitHub Pages 사이트. main 푸시 → 워크플로우가 gh-pages로 미러링 → 자동 배포.
URL: https://mkrealestate4s.github.io/mynews/

## 구조
- `index.html` — 홈(리포트 목록). 새 글은 목록 맨 위에 카드 추가.
- `posts/YYYY-MM-DD-slug.html` — 리포트 본문. 기존 포스트를 복제해 스타일 유지.
- `images/cards/{report,white,editorial}/<slug>-*.png` — 카드뉴스 (테마별 동일 파일명).
- `themes/design-library.json` + `themes/preview.html` — 디자인 프리셋 라이브러리/카탈로그.
- `tools/make_cards.py`, `tools/fetch_fonts.py` — 카드뉴스 생성 (스킬 `.claude/skills/theme-preset` 참조).
- `title.txt` — **당일 블로그 제목 추천 3줄만**(주석·빈줄 없음, UTF-8 BOM 없음, LF).
  로컬 포스팅 자동화 프로그램이 `https://mkrealestate4s.github.io/mynews/title.txt`로
  받아쓴다 → 형식을 바꾸지 말 것. 프로그램은 **첫 줄(1번 제목)만 사용**하며 날짜별로 한 줄씩
  보관한다. `titles-archive.txt`는 날짜·리포트 번호가 붙은 누적 기록(사람용, 밀린 제목 회수용).
- `publish/` — 로컬 포스팅 프로그램용 산출물. `manifest.json`(발행 지시서: status·mode·제목·
  카드 URL·태그), `queue.json`(발행 후보 목록 — 여러 편을 몰아 처리할 때), `<slug>.txt`(발행용
  본문 — [이미지N] 자리표시 유지, 작업용 안내 꼬리 제외). `tools/naver_fetch_job.py`가 이것들을
  읽어 블록 분해·이미지 다운로드·중복 방지(posted.log)를 처리한다. robots.txt에서 색인 제외.
- 테마 3종(report/white/editorial)은 CSS 변수 + `localStorage['mynews-theme']` 공유.

## 데일리 리포트 작성 절차
1. WebSearch로 당일 부동산 이슈 검색 → 데이터가 풍부한 키워드 1개 선정.
2. 본문: 대시보드(kcard 4 + 차트) → 카드뉴스 갤러리 → 기사(섹션별 차트) → 체크포인트 3 → 출처.
3. 카드뉴스 6장 × 3테마 생성(스킬 참조), 인덱스에 글 카드 추가, 커밋·푸시, 배포 success 확인.
4. **SEO**: 새 글마다 `sitemap.xml`과 `feed.xml`에 항목 추가, 포스트 head에
   canonical/og:url/JSON-LD(NewsArticle) 포함 (기존 포스트 복제 시 URL·제목·날짜 교체 확인).
5. **제목 추천 파일**: 사용자 보고에 넣는 블로그 제목 3개를 `title.txt`에 **덮어쓰고**
   (3줄만, 주석 금지 — 로컬 프로그램이 그대로 읽는다), `titles-archive.txt` 맨 위(주석 블록 아래)에
   `날짜 · #번호 · 키워드 · 포스트경로` + 1)2)3) 블록을 추가한다. 커밋에 함께 포함.
   - **1번 제목이 실제 발행에 쓰인다**(프로그램이 첫 줄만 가져감) → 1번은 마감·핵심 수치를 담아
     그대로 올릴 수 있는 완성형으로. 2번은 후킹형, 3번은 실용형.
   - **하루에 2편 이상 낼 때**: `title.txt`는 한 칸뿐이고 로컬 프로그램도 날짜별 한 줄만 보관하므로
     **마감이 있는 글을 `title.txt`에 넣는다**. 마감 없는 글은 아카이브에만 남기고 사용자에게
     "나중에 올릴 후보"로 보고한다(덮어써서 유실되지 않게).
   - 커밋·배포는 보통 07:13~07:26에 끝난다(로컬 프로그램은 07:40 이후 받아감). 크게 늦어지면
     사용자에게 알린다.

## 특집·안내성 글 (공고 안내 등 비데일리)
- 리포트 번호는 데일리와 이어서 붙이고 eyebrow에 `· 특집`을 덧붙인다(예: `리포트 #35 · 임대주택 특집`).
  번호는 index.html 최상단 카드에서 읽으므로 자동으로 맞는다.
- **마감이 있는 글은 뉴스가 아니라 실행 가이드로 쓴다.** "작년 대비 이렇게 바뀌었다"는 발표 시점의
  뉴스라 며칠만 지나도 시의성이 죽는다. **일정 → 달라진 점 → 함정 → 체크리스트** 순으로 배치하고
  헤더·대시보드·정리에 마감일을 반복 노출한다. 전년 대비 수치는 헤드라인이 아니라 "왜 지금 다시
  봐야 하나"의 근거로 뒤에 둔다. 검색 수요는 발표일이 아니라 **접수·마감 직전에 몰린다.**
- 외부 도구·계산기 링크는 **단독 줄**로 넣는다(네이버 에디터가 링크 카드로 렌더). 원문(깃페이지)
  링크 금지 원칙은 그대로 적용.
- 넘겨받은 브리프에 "미확인"으로 표시된 항목은 글에서 **단정하지 않는다** — 별도 확인 안내 문구로 처리.
- 초안 단계에서는 `index.html`·`sitemap.xml`·`feed.xml`에 등록하지 않고 푸시해 미공개로 검토받는다
  (홈·검색·RSS 노출 없음, 컨테이너 회수 대비 원격 보관 겸용). 승인 후 세 파일에 등록.

## 데이터 시각화 표준 (중요 — 사용자 요구사항)
- **촘촘하게**: 헤드라인 숫자 2개 비교로 끝내지 않는다. 가능하면
  **시계열(4+ 데이터 포인트)**, **다중 지표(매매/전세/월세 등)**, **전기 대비 병기**를 쓴다.
- 모든 수치는 **보도·통계 출처가 있는 실제 값만** 사용(임의 보간·창작 금지). 출처는 푸터에 표기.
- 차트 컴포넌트: 가로 막대(.chart/.bar/.rail/.fill), 세로 컬럼 시계열(.colchart/.cols/.col, 눈금
  repeating-gradient), 대형 스탯(.statrow). 회색 막대는 --fillgray(트랙과 구분), 값 라벨은 짧은
  막대 밖에 배치(.railval).
- 단위·기간이 다른 수치를 한 축에 섞지 않는다(주간/분기/연간 분리).
- **형태 다양화**: 인접한 차트·카드가 같은 형태(가로 막대 연속 등)로 반복되지 않게 한다.
  시계열→세로 컬럼, 크기 비교→가로 막대, 헤드라인→대형 스탯.
  전/후 변화는 **성격에 맞게**: 한 번에 점프한 단일 사건(재계약 등)은 계단형(step, 점선 수직 점프)으로,
  연속 추이의 양 끝 비교만 있을 때만 슬로프로. 두 점을 직선으로 이으면 중간 추세로 오독될 수 있음.
  차트 주석 라벨은 막대 높이에 영향 없는 절대 위치 레이어에 그린다.
- 카드뉴스도 그래프 우선: 표지+흐름+데이터 3~4장+체크리스트, 6장 구성 기본.

## 카드뉴스 가공 원칙 (중요 — 사용자 요구사항)
카드는 기사가 아니라 "3초 안에 읽히는 그림"이다. 본문은 촘촘하게, 카드는 쉽게 — 역할 분리.
- **한 카드 = 결론 한 문장(제목) + 그림 하나.** 메시지 2개를 한 카드에 넣지 않는다.
- **통계 단위를 생활 단위로 환산**: "주간 변동률 0.32%" → "5억 전세면 매주 +160만원",
  "지수 463(2022=100)" → "4.6배", "월 +50만원" → "연 +600만원". 환산 근거는 카드 하단 ※주석 한 줄.
- **제목은 결론형**으로: "주간 전세가격 변동률" ❌ → "5억 전세라면, 매주 이만큼" ⭕
- 전문용어(트리플 강세, QoQ, 지수) 대신 쉬운 말. 괄호 병기(1분기 +0.24 등)는 카드에서 제거.
- 흐름 설명은 텍스트 문단 대신 **번호 흐름도(1→2→3)**로.
- **정렬 규칙**: 모든 텍스트는 공통 왼쪽 라인에 정렬. 대형 스탯의 캡션은 옆에 세로 중앙으로
  띄우지 말고 **아래 줄에 왼쪽 정렬**로. 그래픽 옆 라벨은 세로 중앙이 아니라 **바닥선(baseline) 정렬**.
  두 줄 캡션은 가급적 한 문장으로 합쳐 줄바꿈 없이.
- **블로그용 복사 매핑**: 본문의 차트/figure에는 `data-img="카드뉴스 N번 · 카드 제목"`을 달아
  복사 텍스트의 [이미지N] 라벨이 실제 카드 번호·제목과 1:1로 일치하게 한다. 표지·흐름도처럼
  본문에 대응 요소가 없는 카드는 `<span class="imgslot" hidden data-img="...">`로 위치를 지정.
  - **[이미지N]의 N은 순번이 아니라 카드뉴스 번호** — blogText()가 data-img의 "카드뉴스 N번"에서
    번호를 파싱한다(2026-07-19 이후 포스트 스크립트 기준). 라벨에 카드 번호를 반드시 포함할 것.
  - **카드 6장 전부 `<article>` 안에 자리가 있어야 한다.** 대시보드 등 article 밖 차트에 대응하는
    카드는 article 안에 hidden imgslot을 따로 둔다. 푸시 전 복사 텍스트에 [이미지1]~[이미지6]이
    모두 나오는지 확인.
  - **이미지 자리는 해당 소제목(H3) 바로 아래**에 둔다(블로그 발행 관행: 소제목→이미지→본문).
    한 섹션에 카드가 2장이면 첫 장은 소제목 아래, 둘째 장은 문단 뒤.
  - **추천 태그**: `<article data-tags="태그1,태그2,...">`에 키워드 태그 10개 내외(키워드·지역명·
    제도명 위주)를 넣으면 blogText()가 복사 텍스트 끝에 `#태그` 줄로 출력하고, 포스트 스크립트가
    복사바 아래에 태그 칩 줄을 자동 렌더한다(칩 클릭 = 태그 하나 복사, 네이버 태그창은 하나씩
    입력하는 구조라 보고 쓰거나 하나씩 붙여넣는 용도). 새 포스트마다 글 주제에 맞게 교체할 것.
  - **복사 텍스트 꼬리 규칙**: 본문 끝에 ① 푸터 1행("참고 자료: ...")을 자동 인용(발행에 포함 —
    데이터 출처는 신뢰도 신호) ② "──── 아래는 작업용 안내 · 발행 전 삭제 ────" 구분선 뒤에
    ※이미지 안내·추천 태그(발행 전 삭제용). **원문(깃페이지) 링크는 절대 포함하지 않는다**
    — 블로그 글의 원본성 보호(네이버 봇 차단 전략과 한 세트).

## 검증 체크리스트 (푸시 전)
- playwright-core(executablePath /opt/pw-browsers/chromium)로 모바일 390px 확인:
  가로 오버플로 없음, 이미지 1:1(aspect-ratio), 게이지 라벨 안 잘림.
- 카드 렌더는 창 1080x1167 → 상단 1080 크롭 (헤드리스 UI 87px 보정).
- 세 테마 모두 스크린샷 확인 후 커밋. 배포는 gh-pages 'pages build and deployment' success 확인.
